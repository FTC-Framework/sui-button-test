{
  "name": "fotocasa-style",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",

    "clean:dist": "rm -rf ./dist/*",
    "predist": "npm run clean:dist -s",
    "dist": "npm run dist:scripts && npm run dist:styles",
    "dist:scripts": "babel src --out-dir dist",
    "dist:styles": "node-sass src/index.scss dist/sui-card.css --importer ./scripts/importer.js",
    "postdist:styles": "cp dist/sui-card.css dist/_sui-card.scss",
    "prepublish": "npm run dist",
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "beepbeep": "^1.2.0",
    "browser-sync": "^2.9.11",
    "colors": "^1.1.2",
    "del": "^2.0.2",
    "gulp": "^3.9.0",
    "gulp-plumber": "^1.0.1",
    "gulp-sass": "^2.0.4",
    "run-sequence": "^1.1.4"
  }
}
